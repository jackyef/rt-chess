/**
 * Shared types between frontend and backend
 * These types define the API contract
 */

export type { BackendGameWebSocketMessage, ClientGameWebSocketMessage } from '../../backend/types/websocket.types'
export type { GameEndedReason, GameState } from '../../backend/types/game.types'
